spring:
  cloud:
    nacos:
      config:
        server-addr: ${nacos.address}:8848
        file-extension: yaml
  application:
      # 服务名称用-划线，不要用_ 或者特殊字符
      name: user-center
  profiles:
    active: prod